modules = ["python-3.11"]

[project]
name = "restaurant-ops-backend"
description = "FastAPI backend for Restaurant Operations Dashboard"
language = "python"

# ✅ Ensure Replit passes its dynamic port
run = "python3 main.py"

[nix]
channel = "stable-25_05"
packages = ["libxcrypt"]

[env]
PORT = "8000" # fallback to 8000 if Replit doesn't inject one

[agent]
expertMode = true

[[ports]]
localPort = 8000
externalPort = "public"
